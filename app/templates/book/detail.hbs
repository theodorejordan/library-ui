<div class="slide-out-bg"></div>

<div class="slide-out">
    <div class="slide-out-card">
        <LinkTo @route="book" class="btn-back">
            <span class="sr-only">Back</span>
        </LinkTo>

        <div class="slide-out-heading">
            <div class="title">
                <h3> {{@model.title}}</h3>

                <div class="actions">
                    {{!-- <LinkTo @route="book.edit" @model={{@model}} class="btn-pop">edit</LinkTo> --}}
                    {{!-- <button class="btn-recessed" onclick={{action 'deleteAuthor' @model}} type="button">Delete</button> --}}
                </div>
            </div>

            <div class="details">
                <p>
                    <strong>Author:</strong>

                    {{@model.author.last}}, {{@model.author.first}}
                </p>
            </div>
        </div>

        <div class="slide-out-content"></div>
    </div>
</div>